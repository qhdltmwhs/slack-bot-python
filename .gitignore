# 가상 환경 (Python venv)
bin/
include/
lib/
pyvenv.cfg
# requirements.txt 파일은 일반적으로 Git에 포함시켜서 다른 개발자가 환경을 재현할 수 있도록 합니다.
# 만약 스크린샷에서 보이는 SLACK-BOT/requirements.txt가 가상환경 내에 있는 것이라면 이 라인은 그대로 두고
# 프로젝트 루트에 별도로 관리하는 requirements.txt가 있다면 그것을 포함시킵니다.
# 현재 스크린샷만으로는 가상환경 자체에 requirements.txt가 포함된 것으로 보입니다.

# 환경 변수 파일
.env

# macOS 시스템 파일
.DS_Store

# Python 관련 파일
__pycache__/
*.pyc

# 런타임 데이터 파일
data/db.csv
data/temp/

# VS Code 설정 (선택 사항 - 팀 프로젝트 시 팀원들과 공유할 설정이 있다면 제외)
.vscode/

# 기타 IDE 파일 (예: PyCharm)
.idea/